# Homebrew tap for Wotold

[Wotold](https://github.com/zdllucky/wotold) — local call recorder for macOS with
transcription, speaker diarization and summaries. Everything runs on device.

```bash
brew tap zdllucky/wotold
brew install --cask wotold
```

Requires **macOS 14.4+ on Apple Silicon**. The bundled speech and language sidecars
are built for arm64 only.

The build is not notarized by Apple, so the first launch is blocked by Gatekeeper:
open System Settings → Privacy & Security and choose "Open anyway".

## How this tap stays current

`Casks/wotold.rb` is regenerated by [`sync-cask.yml`](.github/workflows/sync-cask.yml),
which polls the main repository's latest release every half hour. The template it
renders lives in the main repository, so packaging changes travel with the code
that produced them.

Nothing pushes into this tap from outside — it pulls. That keeps the release job,
which already carries the update signing key, free of any cross-repository token.
